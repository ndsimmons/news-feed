# Cloudflare Workers Configuration

name = "news-feed"
main = "workers/api.ts"
compatibility_date = "2024-01-01"

# D1 Database
[[d1_databases]]
binding = "DB"
database_name = "news-feed-db"
database_id = "" # Will be filled after creating database

# KV Namespace (for caching)
[[kv_namespaces]]
binding = "KV"
id = "" # Will be filled after creating KV namespace

# Cron Triggers - fetch articles every hour
[triggers]
crons = ["0 * * * *"]  # Every hour at minute 0

# Environment Variables
[vars]
ENVIRONMENT = "development"

# Production environment
[env.production]
name = "news-feed-production"
vars = { ENVIRONMENT = "production" }

# Routes - configure after deploying to Cloudflare Pages
# routes = [
#   { pattern = "news-feed.pages.dev/api/*", zone_name = "pages.dev" }
# ]
